{
  "name": "airport-now",
  "private": true,
  "workspaces": [
    "web",
    "backend"
  ],
  "scripts": {
    "dev:web": "bun run --cwd web dev",
    "dev:api": "bun run --cwd backend dev",
    "build:web": "bun run --cwd web build",
    "build:api": "bun run --cwd backend build"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  }
}
