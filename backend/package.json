{
  "name": "airport-pulse-api",
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.3"
  },
  "private": true,
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build ./src/server.ts --outdir dist --target bun"
  },
  "type": "module",
  "dependencies": {
    "@vercel/blob": "^2.3.1"
  }
}
